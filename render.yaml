services:
  - type: web
    name: usa-truck-dispatch-bot
    runtime: python
    plan: free
    pythonVersion: 3.12.10
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
